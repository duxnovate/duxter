{
  "name": "your_app",
  "module": "main.ts",
  "version": "1.0.0",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "author": "duxnovate",
  "license": "MIT",
  "scripts": {
    "dev": "bun run main.ts --watch",
    "build": "bun build main.ts --bundle --outdir dist --minify",
    "migrate": "bun run src/database/config/migrate.ts",
    "env": "cp .env.example .env"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "fastify": "^5.0.0",
    "knex": "^3.1.0",
    "pg": "^8.13.0",
    "pino-pretty": "^11.2.2"
  }
}
